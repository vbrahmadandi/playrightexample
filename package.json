{
  "name": "playwright-docker-githubactions",
  "version": "1.0.0",
  "description": "Sample Playwright project with Docker and GitHub Actions",
  "main": "index.js",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
